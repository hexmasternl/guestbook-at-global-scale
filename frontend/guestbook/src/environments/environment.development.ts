// Development environment configuration. Points at HexMaster.Guestbook.Api's
// local `https` launch profile (see src/HexMaster.Guestbook.Api/Properties/launchSettings.json).
export const environment = {
  production: false,
  apiBaseUrl: 'https://localhost:7071',
};
